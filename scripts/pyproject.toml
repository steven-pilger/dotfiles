[tool.poetry]
name = "scripts"
version = "0.1.0"
description = ""
authors = ["Steven Pilger <stevenpilger@fastmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"
ipython = "^7.4"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
